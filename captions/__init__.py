"""Platform-optimized caption generation (stdlib only).

Captions are built from opinionated hook formulas + platform norms — not
generated prose — so they stay deterministic and auditable. Every caption
goes through the standard content gates (secrets, identity, voice) in the
CLI before it is ever drafted: human-sounding and written as the owner.
"""
